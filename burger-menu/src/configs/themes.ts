import { themeLight } from '../styles/themes/themeLight'
import { themeDark } from '../styles/themes/themeDark'
import { themeContrast } from '../styles/themes/themeContrast'

export const themes = {
  light: themeLight,
  dark: themeDark,
  contrast: themeContrast,
}